"""Everything the patcher needs to open, rewrite and restore the game's data.

Kept apart from the GUI so the whole thing can be driven headlessly
(`patcher.py --install`) and tested without a display.
"""
